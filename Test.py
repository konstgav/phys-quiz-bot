class Test:
	def __init__(self, name, info, questions, maxscore, minscore, timeinseconds):
		self.Name = name
		self.Info = info
		self.Questions = questions
		self.MaxScore = maxscore
		self.MinScore = minscore
		self.TimeInSecinds = timeinseconds
		
